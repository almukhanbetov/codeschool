import type { Metadata } from "next";
import { ForParentsPage } from "@/components/public/ForParentsPage";

export const metadata: Metadata = { title: "For parents — CODESCHOOL" };

export default function Page() {
  return <ForParentsPage />;
}
